"""Generated protobuf + gRPC stubs are placed in this package at image build time.

The Dockerfile runs ``python -m grpc_tools.protoc`` during the build step and
outputs the generated files here.  This file makes the directory a regular
Python package so imports resolve at runtime.

Do **not** commit the generated ``*_pb2*.py`` files — they are build artefacts.
"""
